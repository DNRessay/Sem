fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
asyncpg>=0.29.0
boto3>=1.34.0
httpx>=0.27.0
python-dotenv>=1.0.0
pypdf>=4.2.0
python-docx>=1.1.0
# Runtime dependency (not just CI) — tools/dev_loop_tool.py shells out to
# `python -m ruff check` to lint an AI-proposed fix before it's ever
# allowed to reach tools/repo_write_tool.py's propose_fix. Same version
# floor as requirements-dev.txt for consistency.
ruff>=0.6.0
