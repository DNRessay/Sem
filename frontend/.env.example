# Local dev: point at your local backend
VITE_API_URL=http://localhost:8000

# Production: your deployed Lambda Function URL (the `ChatFunctionUrl`
# output from `sam deploy`), no trailing slash
# VITE_API_URL=https://xxxxxxxx.lambda-url.us-east-1.on.aws
